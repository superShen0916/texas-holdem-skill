# MAINTAINING — 维护指南

**读者：来修改这个仓库的 AI agent（或人）。**

- 如果你是要**用这个库打牌** → 读 [`AGENTS.md`](AGENTS.md)
- 如果你是要**改这个库** → 你在读对的文件

---

## 0. 先理解这个仓库是什么

| 项 | 说明 |
| --- | --- |
| **产品** | 一个德扑策略知识库。**读者是 LLM agent**，不是人 |
| **不是** | 不是程序。本仓库没有可执行代码（`scripts/` 里只有校验脚本）、没有牌局接口、不实现 agent |
| **核心约束** | 读者 LLM 有两个硬毛病：**不会心算**、**不会通读** |

**整个仓库的形状都是为了绕开这两点。改任何东西之前先理解这一条**，否则你很可能好心改坏（见第 5 节）。

---

## 1. 仓库地图

| 路径 | 作用 | 谁读 |
| --- | --- | --- |
| `AGENTS.md` | agent 入口 + 角色路由 | 用库的 agent |
| `DECISION.md` | 决策流程：处境 → 动作四步 | 用库的 agent |
| `GLOSSARY.md` | 术语 + 范围记号规范 | 用库 / 改库 |
| `reference/` | **预计算表，全库唯一数字来源** | 用库的 agent |
| `01`–`05`, `07-*` | 策略知识（翻前/翻后/尺度/数学/剥削/玩法） | 用库的 agent |
| `tests/spots.md` | 测点集：给 agent 打分用 | 改库时验证 |
| `human/` | 心态 / 资金 / 工具 | **人**，agent 不用 |
| `scripts/` | 校验脚本（CI 跑） | 改库 |
| `CONTRIBUTING.md` | 加内容的格式规范 | 改库 |
| `README.md` | 面向人的项目门面 | 人 |

---

## 2. 铁律（违反即视为 bug）

| # | 铁律 | 为什么 | 怎么保证 |
| --- | --- | --- | --- |
| I1 | **数值只存在于 `reference/`** | 重复必然漂移，agent 会读到互相矛盾的数字 | 代码审查；公式表放 `reference/`，模块只链接 |
| I2 | **每条规则带完整条件**（位置 + 筹码 + 场景 + 面对） | agent 会忽略隐含条件 | 用 `CONTRIBUTING.md` 的规则表格式 |
| I3 | **每个断言带置信度标记** | agent 靠它决定多信这条 | `[数学]/[共识]/[常见]/[经验]/[存疑]` |
| I4 | **显式声明覆盖边界** | 有损压缩必须让损失可见，否则 agent 会编 | 每个文件末尾写"未覆盖" |
| I5 | **内部链接必须有效** | 链接断了，路由就断，agent 只能瞎编 | `scripts/check_links.py` + CI |
| I6 | **数字必须能被重算出来** | agent 直接信任表里的值 | `scripts/verify_tables.py` + CI |

> I5/I6 由 CI 强制，合不进去。**别试图绕过**——它们挡的是最难发现的错误。

---

## 3. 改东西的配方

### 加一个数值
1. 加到 `reference/` 里对应的表（没有合适的表就新建，并在 `AGENTS.md` 路由里登记）
2. **同时**在 `scripts/verify_tables.py` 里加一条断言，让公式验它
3. 跑 `python3 scripts/verify_tables.py`
4. 别在模块文件里复制这个数字——**链接过去**

### 加一条规则
1. 找到对应的模块文件
2. 按 `CONTRIBUTING.md` 的规则表格式写（先读那张表的列定义）
3. 条件必须写全；数值引用 `reference/`；打置信度标记
4. 顺手加一个对应测点到 `tests/spots.md`（**加了规则不加测点，就无法验证它起作用**）

### 加一个新模块
1. 建目录 + 一个描述性文件名（**不要用 `README.md`**，见第 5 节）
2. 在 `AGENTS.md` 路由表里登记——**没登记 = agent 找不到 = 等于没写**
3. 在 `README.md` 结构图里登记
4. 跑 `scripts/check_links.py`

### 改一个范围（最危险的操作）
1. 范围用 [`GLOSSARY.md`](GLOSSARY.md) 的标准记号
2. **自己按 `+` / `-` 规则展开一遍**，核对组合数与百分比（`reference/combinatorics.md`）
3. 拿不准就加括号注明展开结果
4. 检查有没有测点依赖这个范围，一起改

### 加一个测点
1. 加到 `tests/spots.md` 对应分类
2. 标准动作要**无歧义**——有争议的点标 `[常见]` 并在说明里注明
3. 边界类测点（H 类）不可或缺：它们考的是 agent **敢不敢说"不确定"**

---

## 4. 验证

```bash
python3 scripts/verify_tables.py   # 所有数字用公式重算，逐格比对
python3 scripts/check_links.py     # 所有内部链接是否有效
```

两个都会非零退出。CI（[`.github/workflows/verify.yml`](.github/workflows/verify.yml)）在每次 push / PR 都跑。

**提交前两个都要过。** 改了数字不跑 verify、改了路径不跑 links，等于把 bug 合进主干。

---

## 5. 为什么是这个形状（**改回去之前先读这节**）

下面每条都是刻意的。看起来"可以更自然"的改法，多半会破坏设计意图。

| 现状 | 常见"改进" | 为什么不能改 |
| --- | --- | --- |
| 数值全在 `reference/` | "就近把数字写在规则旁边更好读" | 重复会漂移，agent 读到矛盾数字后无法自洽 |
| 大量用表格 | "写成段落更通顺" | LLM 解析表格比散文稳，散文的限定词会丢 |
| 每条规则标注信度 | "标注太啰嗦" | agent 需要知道该不该引用、要不要提示用户 |
| 每个文件写"未覆盖" | "这是自曝其短" | **边界可见是让有损压缩变安全的唯一机制** |
| `AGENTS.md` 顶部有角色路由 | "直接写使用说明就行" | 按约定 `AGENTS.md` 是给"改仓库的 agent"的；不路由会误导维护者 |
| 模块文件用描述性名，不用 `README.md` | "目录里放 README 是惯例" | agent 随机访问时，文件名就是索引；8 个 `README.md` 无法区分 |
| `human/` 单独分开 | "反正 agent 读了也没坏处" | 混在一起会稀释路由，还可能让 agent 把"别上头"当规则用 |
| 范围标 `≈` 而非精确值 | "给个确定数字更专业" | 本库没有求解器背书，**假装精确是撒谎**；标近似才诚实 |

---

## 6. 已知缺口（欢迎接手）

按重要性排：

1. **策略内容未经求解器验证。** 范围与频率是主流共识的近似，不是 GTO 精确解。有求解器的话，这是最值得补的。
2. **测点集没有自动跑分器。** `tests/spots.md` 是题库，但把局面喂给 agent、比对动作、算通过率这层还没写。
3. **没有一手牌级别的完整算例。** 从翻前到河牌走一遍，最能检验 `DECISION.md` 是否走得通。
4. **`reference/` 里没有"手牌 vs 范围"的权益表。** 这类需要枚举，当前一律指向外部计算器。
5. **翻前范围没有附组合数核对。** 目前只给了近似百分比。

> 接手时建议从 **2** 或 **3** 入手：它们是"能验证整套设计是否成立"的那种工作。
