# 贡献规范

本库是**给 AI agent 用的知识库**，所以对格式的要求比普通文档严格：格式不一致，agent 就会读错。

---

## 第一条：数值不许重复造

所有数值（赔率、权益、组合数、SPR、MDF）**只能存在于 [`reference/`](reference/)**。

- 需要某个数值 → **链接**到对应表，不要在模块里复制一遍
- 发现表里缺某个档位 → 加到 `reference/` 的表里，其他地方引用它

理由：数值散落各处必然漂移，最后 agent 读到互相矛盾的数字。

改完任何数字，**必须**跑一遍校验：

```bash
python3 scripts/verify_tables.py
```

这个脚本会用公式重算 `reference/` 里的每一个值并逐格比对，对不上就非零退出。CI 也会在每次提交时跑它——数字错了合不进去。

---

## 第二条：规则用统一表格

各模块的决策规则一律用这种格式：

| 位置 | 筹码深度 | 牌面 / 场景 | 面对 | 动作 | 尺度 | 依据 | 置信度 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| BTN | 100bb | 翻前 | CO open | 3-bet 或弃 | 3× | 阻断 + 翻后可打 | `[共识]` |

**列的含义**

- **位置**：UTG / HJ / CO / BTN / SB / BB（见 [`GLOSSARY.md`](GLOSSARY.md)）
- **筹码深度**：有效筹码，如 `100bb`、`20-40bb`、`<10bb`
- **牌面 / 场景**：翻前场景，或翻牌面类型（干燥 / 湿润 / 连接 / 同花 / 成对，定义见 [`GLOSSARY.md`](GLOSSARY.md)）
- **面对**：对手的行动
- **动作**：open / fold / call / 3-bet / c-bet / check-raise / all-in 等
- **尺度**：下注比例（相对底池）或倍数
- **依据**：为什么
- **置信度**：见下

**条件必须写全**。宁可拆成多行，也不要把条件合并——agent 会忽略隐含条件。

---

## 第三条：置信度标记

| 标记 | 用在 |
| --- | --- |
| `[数学]` | 可由数学证明的结论（赔率、概率） |
| `[共识]` | 主流公认、教科书级 |
| `[常见]` | 常见打法，但存在合理分歧 |
| `[经验]` | 个人实战经验，未经严格验证 |
| `[存疑]` | 有争议、可能过时、待验证 |

**诚实优先**：不确定就标 `[经验]` 或 `[存疑]`，**不要**把没验证的东西标成 `[共识]`。标错比不标危害大。

---

## 第四条：范围用标准记号

范围一律用 [`GLOSSARY.md`](GLOSSARY.md) 定义的标准记号（`22+`、`A5s-A2s` 等）。

- 写完**自己展开一遍**核对组合数（见 [`reference/combinatorics.md`](reference/combinatorics.md)）
- 范围后**附上百分比**（以 1326 为分母），作为展开是否正确的抽查点
- 拿不准的加括号注明展开结果，如 `ATs+ (ATs, AJs, AQs, AKs)`

---

## 第五条：给 agent 写，不是给人写

- **别用"如上所述""前面提过"**——agent 是随机访问，不保证读到上下文
- **每个文件自包含**：需要的定义和链接都写清楚
- **表格优于段落**：agent 解析表格比散文稳
- **一条规则一个条件组合**，不要写"通常……但在……时……"

---

## 提交前自查

- [ ] 数值是否都链接到 `reference/`，没有复制
- [ ] 规则是否都有**完整条件**（位置 + 筹码 + 场景 + 面对）
- [ ] 是否标了**置信度**
- [ ] 范围记号展开后组合数/百分比是否对得上
- [ ] 涉及的术语是否都已在 [`GLOSSARY.md`](GLOSSARY.md) 定义
- [ ] 新术语是否补进了 `GLOSSARY.md`
